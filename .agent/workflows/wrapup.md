---
description: Wrapup by committing and pushing
---

Commit and push to github

Be sure to update game version using 4 point version v.x.y.z where

v=version (currently 0)
x=subversion (currently 9)
y=commit number (taken from git's commit value)
z=iteration increment per prompt